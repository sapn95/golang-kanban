# The build stage does COPY . . — keep the context to the source it needs.
.git
.github
.gitignore
.dockerignore

# Local build and test output; a host-built binary must never reach the image.
/kanban
coverage.out

# The CSS is compiled and committed, so the image needs neither the compiler
# nor its inputs. See docs/adr/0011.
/.tools/
assets/tailwind.sh
assets/tailwind.config.js
assets/tailwind.input.css

docs
readme.md
assets/Screenshot_v1.0.0.png
